# Consule root environment example
# Copy this file to .env and adjust values for your server

# Your public domain (DNS A/AAAA should point to your server)
DOMAIN=consule.cloud

# Base URL where your backend API is reachable by the browser
# Example if your API runs on the same host behind this proxy: https://consule.cloud/api
# Or direct backend port: http://<server-ip>:3000
API_BASE_URL=https://consule.cloud

# Optional display-only values consumed by env.js
ENV_NAME=production
APP_VERSION=0.0.0

# Optional: name for the local image tag built by compose (kept simple)
DOCKER_IMAGE=consule-frontend
